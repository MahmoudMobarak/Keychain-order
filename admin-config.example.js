// Copy this file to admin-config.js and set your own hash.
// Generate hash quickly in browser console: 
// (()=>{let h=0,s='your-password';for(let i=0;i<s.length;i++){h=(h<<5)-h+s.charCodeAt(i);h|=0;}console.log(h.toString())})();
window.ADMIN_PASSWORD_HASH = "CHANGE_ME";
